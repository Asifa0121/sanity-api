import { title } from "process";
import { defineType, defineField } from "sanity";

export const Product={
    name:'product',
    type:'document',
    title:'product',
    fields:[
        {
            name:
        }
    ]

}